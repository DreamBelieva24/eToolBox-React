import React from "react";

const Nav = (props) => (
  <div className="row align-center padded animated bounceInDown">
    <div className="one fifth warning box skip-two"><h1>{props.name}</h1></div>
     
    
  </div>
);

export default Nav;
